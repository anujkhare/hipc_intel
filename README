Team: kcm

Code file: kmeans.cpp
Input file: "data.csv"
Output file: "kcm_phase1.csv"

TO RUN THE CODE:
1. Compile using C++11 compiler:
   g++ kmeans.cpp -o kmeans -std=c++11

2. Run the compiled file:
   ./kmeans

Note:- You can change these paths in the first few lines of the c++ code
	 - RAND_SEED sets the random number generator seed
